# Sample app R8 rules. Console itself ships its (minimal) consumer rules in its AARs,
# so nothing Console-specific is needed here — this validates that the real Console
# implementation (devRelease variant) shrinks and obfuscates cleanly.
